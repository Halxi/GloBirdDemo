@model IEnumerable<GloBirdDemo2.Models.CallNote>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CallNoteId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ParentCallNoteId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Customer.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Text)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateTime)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CallNoteId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ParentCallNoteId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.Username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Text)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateTime)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.CallNoteId }) |
                @Html.ActionLink("Details", "Details", new { id = item.CallNoteId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.CallNoteId })
            </td>
        </tr>
    }

</table>

